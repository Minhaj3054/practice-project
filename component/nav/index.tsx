import Link from 'next/link';
import React from 'react';

const Navbar = () => {
    return (
        <nav>
            <Link href={'/'}>Gallery</Link>
        </nav>
    );
};

export default Navbar;